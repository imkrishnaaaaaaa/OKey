{
  "name": "okey",
  "version": "1.0.0",
  "description": "OKey — A premium zero-knowledge password & 2FA manager backed by your own Google Sheet.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:extension": "node build.mjs extension",
    "build:pwa": "node build.mjs pwa",
    "watch": "node build.mjs --watch"
  },
  "keywords": [
    "password-manager",
    "chrome-extension",
    "pwa",
    "encryption",
    "zero-knowledge",
    "argon2",
    "totp",
    "okey"
  ],
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "@scure/bip39": "^1.6.0",
    "chart.js": "^4.5.1",
    "hash-wasm": "^4.12.0"
  }
}
