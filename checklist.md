# UI/UX & Master PIN Checklist

- [x] 1. Change the caption from "One key to rule them all" to a professional industry-standard caption.
- [x] 2. Fix the "Reset vault & start fresh" button styling so it doesn't look odd.
- [x] 3. Fix the spacing between "Create new vault" and "Restore existing vault" buttons so they don't stick together.
- [x] 4. Change the Master Password to a 4-digit Master PIN across the app.
- [x] 5. Implement PIN auto-focus and auto-submit UI (auto-move to confirm, auto-submit on 4th digit, auto-verify match).
- [x] 6. Update Password Strength Meter from 5 levels to 4 levels (colors: red, orange, green, purple).
- [x] 7. Update onboarding message to reflect 4-digit PIN, remove em-dashes, and remove the "Minimum 10 characters" text.
- [x] 8. Improve Toast notification styling (better colors, blur, fix z-index so it shows above overlays).
- [x] 9. Add visual loaders to "Setup sheet" and "Sync now" buttons during API calls.
- [x] 10. Improve Settings section UI (fix borders, adjust button curves to be less round, add icons to export/import).
- [x] 11. Enhance the Export warning message to clearly state that the JSON contains real passwords and advise deletion.
- [x] 12. Update global theme border-radius from fully circular/pill-shaped to a modern curved square (squircle/Windows-like) look.
